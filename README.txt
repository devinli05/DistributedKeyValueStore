Special instructions for compiling/running the code should be included in this file.

1. Use go get to fetch memberlist and govec from github.
2. Use bash to run setupNodes.sh to use config.json to start 8 processes on localhost.
3. Copy linuxnode.json to config.json and use bash to run setupLinuxNodes.sh to start on lin10-lin17.ugrad.cs.ubc.ca

Note: the user has to be able to ssh into ugrad server to run on ugrad linux machines

The End.
